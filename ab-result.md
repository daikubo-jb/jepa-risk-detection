---
title: V-JEPA 2 経路A/B 全件validation結果レポート
date: 2026-09-15
status: exploratory-validation-complete-official-test-pending
---

# V-JEPA 2 経路A/B 全件validation結果レポート

English version: [`ab-result.en.md`](ab-result.en.md)

## 結論

Nexar Collision Predictionの内部validation 300動画・21,554窓を使い、同じフレーム集合で経路Aと経路Bを比較した。両方とも一定スコア基準を上回ったが、リスク区間を順位づけする性能はBの方が高かった。

| 経路 | 何を判定するか | ROC-AUC | Average Precision | 一定スコア基準AP |
|---|---|---:|---:|---:|
| A | 前半特徴から後半特徴を予測した誤差 | 0.633691 | 0.037674 | 0.024011 |
| B | 観測済み窓のglobal特徴からリスク区間を分類 | 0.801785 | 0.144671 | 0.024011 |

共通フレームではA/Bとも32万1,931件を評価した。BのAPは一定スコア基準の約6.03倍、Aの約3.84倍だった。この結果は「現在の入力・前処理・ラベル定義で、固定V-JEPA 2からリスク区間と相関する情報を取り出せるか」を調べた探索的validationの結果であり、事故の予知性能や実用的な警告器の完成を示すものではない。

## 1. 目的とA/Bの違い

目的は、MetaのV-JEPA 2を追加学習せずに固定したまま、Nexarのラベル付き動画でリスク区間を識別できるかを二つの経路で比較することである。

### A: マスク予測誤差

#### 仮説

JEPAが前半の映像から後半の映像を予測しにくかった区間は、通常と異なる状態であり、リスク区間ではないか、という仮説である。

#### 手法

64フレームの2秒窓を前半48フレームと後半16フレームに分け、次の値を計算した。

1. 前半48フレームをcontextとして、predictorが後半16フレームの特徴量を予測する。
2. 実際の後半16フレームをtarget encoderに入力し、正解側の特徴量を得る。
3. 予測特徴量と正解特徴量の平均L1距離を計算する。
4. この距離を「予測のしにくさ」を表すAスコアとする。

つまりAスコアは、risk / not riskの分類器が出す確率ではなく、次のような予測誤差である。

```text
Aスコア = 平均 |予測された後半特徴量 - 実際の後半特徴量|
```

#### 何を正解と比較したか

JEPAの予測結果をrisk / not riskとして直接正答判定したわけではない。まず上記の予測誤差を各窓について求め、その後で公式ラベルと比較した。

- 公式リスク区間内の窓: 正例
- 公式リスク区間外の窓: 負例

そして、「リスク区間の窓ほどAスコアが高いか」をROC-AUCとAverage Precisionで確認した。したがってAには通常の意味での分類正答率はなく、連続スコアとriskラベルの対応を評価している。

このスコアは後半16フレームを観測した時点で確定する予測誤差である。したがって、前半映像だけを見て将来の事故を先行警告するモデルではない。また、窓間の特徴量差分を直接計算した実験でもない。

#### 結果

ROC-AUCは`0.633691`、Average Precisionは`0.037674`だった。一定スコア基準のAP`0.024011`を上回り、予測誤差とリスクラベルには一定の相関が確認された。ただしBより性能は低く、誤報も多かった。

### B: 凍結特徴 + 線形probe

#### 仮説

JEPAの予測誤差は使わず、JEPAが映像から取り出した特徴量だけでも、リスク区間と非リスク区間を分類できるのではないか、という仮説である。

#### 手法

1. 観測済み64フレームを固定V-JEPA 2に入力する。
2. 出力特徴を空間・時間方向に平均し、1024次元のglobal特徴にする。
3. Nexarのtrainラベルを使い、`StandardScaler`と線形ロジスティック回帰（probe）を学習する。
4. validationの各窓に対して、リスクらしさを表す連続スコアを出力する。

学習するのはprobe部分だけで、V-JEPA 2本体は固定した。BはJEPAの「次の特徴を予測する機能」は使わず、特徴抽出器として利用する。

#### 何を正解と比較したか

各窓のprobeスコアを、Aと同じ公式リスクラベルと比較した。Bではラベルを使ってprobeを学習しているため、Aと異なり教師あり分類である。ただし、最終的な評価は単純な正答率ではなく、ROC-AUCとAverage Precisionを中心に行った。

Bは、特徴量の時間的な大きな変化を直接検出する実験ではない。観測窓そのものの特徴から、risk / not riskを分類する実験である。

#### 結果

ROC-AUCは`0.801785`、Average Precisionは`0.144671`だった。一定スコア基準のAP`0.024011`を大きく上回り、今回の条件ではAよりもリスク区間を分類する性能が高かった。

## 2. データとラベル

Nexarの公式`train` 1,500動画（positive 750、negative 750）を、動画単位で内部train 1,200本とvalidation 300本へ固定分割した。公式testは取得していない。

positive動画のリスク区間は、公式メタデータの`time_of_alert`と`time_of_event`から半開区間`[time_of_alert, time_of_event)`として定義した。窓末尾の判定時刻`t`がこの区間内なら正例、それ以外なら負例とした。negative動画は全窓が負例である。

| データ | 動画数 | 有効窓数 | 正例窓 | 負例窓 | 用途 |
|---|---:|---:|---:|---:|---|
| 内部train | 1,200 | 86,365 | 1,898 | 84,467 | Bのscaler・probeのfit |
| 内部validation | 300 | 21,554 | 519 | 21,035 | C・閾値選定とA/B評価 |
| 公式test | 0 | 0 | - | - | 未取得 |

判定時刻は2.0秒から0.5秒ずつ進めた。各スコアは窓末尾`t`に対応し、フレーム評価では`t <= u < t+0.5`のフレームへ因果的に割り当てた。全340,025フレーム行のうち321,931行が有効で、coverageは0.946786だった。

## 3. 評価結果

### フレーム単位の順位評価

以下はA/Bの両方が有効な共通フレーム集合での比較である。正例7,730、負例314,201で、正例率に基づく一定スコア基準APは0.024011だった。

| 指標 | A | B |
|---|---:|---:|
| 共通評価フレーム数 | 321,931 | 321,931 |
| ROC-AUC | 0.633691 | 0.801785 |
| Average Precision | 0.037674 | 0.144671 |
| APの動画bootstrap 95%区間 | [0.030654, 0.046513] | [0.112067, 0.189306] |
| bootstrap回数 / seed | 1,000 / 42 | 1,000 / 42 |

A/Bのvalidなフレーム集合は今回一致しているため、単独評価の指標と共通評価の指標は同じ値になっている。これは評価時刻と欠損の扱いを揃えたことを確認するための比較であり、経路間の有意差検定ではない。

### 閾値付きイベント評価

閾値はvalidation上で経路ごとにF1最大となる値を選んだ。したがって以下も同じvalidationで閾値を調整した探索的な結果である。

| 指標 | A | B |
|---|---:|---:|
| 選択閾値 | 0.583661 | 0.899668 |
| 閾値選択時F1 | 0.079653 | 0.226540 |
| 評価イベント数 | 154 | 154 |
| 検出イベント数 | 82 | 80 |
| 検出率 | 0.532468 | 0.519481 |
| 平均検出遅延 | 0.721秒 | 1.051秒 |
| 誤報数 | 985 | 161 |
| 誤報率 / valid秒 | 0.094263 | 0.015407 |

検出率だけではAがわずかに高いが、Aは誤報が大幅に多い。閾値・アラーム連結規則・同一validationという条件を含む値なので、実運用経路の優劣を確定するものではない。

## 4. 解釈

- Bは、観測済み2秒窓のglobal特徴からリスク区間を順位づけする目的では、Aより強い結果になった。
- Aも一定スコア基準を上回ったため、固定predictorの予測誤差にリスクラベルと相関する情報が含まれる可能性はある。ただしAUC/APはBより低く、閾値付きでは誤報が多い。
- A/Bとも、未来情報を使って過去へスコアを割り当ててはいない。Aのtargetはスコア確定時点で観測済みである。
- この結果から、特徴量の「大きな変化」や事故直前の予兆が検出できたとは言えない。差分・変化率・先行予測は別の実験として設計する必要がある。

## 5. 制約と次の評価

公式testは未取得で、C・閾値の選定と評価を同じvalidationで行っている。そのため、今回の数値は未使用データへの最終評価ではない。一般化性能、安全性、実運用の警告性能については結論を出さない。

次に行う最終評価では、trainで学習したB probeと、validationで固定したC・閾値を変更せず、未使用holdoutまたは公式testへ一度だけ適用する。先行警告を検証する場合は、事故時刻より前だけを入力に許すラベル・利用可能時刻・評価指標を別途定義する。

## 再現用成果物

- 統合評価: `artifacts/evaluation-p6-full-ab/metrics.json`
- 統合レポート: `artifacts/report-p6-full-ab/report.md` / `report.json`
- Aスコア: `artifacts/prediction-p6-validation/scores.json`
- Bスコア: `artifacts/scores-p6-full/scores.json`
- prepared manifest: `artifacts/prepare-nexar-v2/`

正式な再生成コマンドはREADMEの`evaluate`と`report`を参照する。生動画、フレーム、特徴量cache、probe重み、動画単位スコアは公開成果物に含めない。公開時の扱いは[`PUBLICATION_POLICY.md`](PUBLICATION_POLICY.md)に従う。
