---
title: V-JEPA 2 経路B（凍結特徴 + 線形probe）結果レポート
date: 2026-09-13
status: exploratory-validation
---

# V-JEPA 2 経路B（凍結特徴 + 線形probe）結果レポート

## まず結論

Bは、V-JEPA 2の特徴量を使って、観測済みの映像がリスク区間らしいかを分類できるか検証した実験である。V-JEPA 2本体は固定し、Nexarのラベル付きtrainデータで線形分類器だけを学習した。

validation全件では、ROC-AUC=`0.801785`、Average Precision=`0.144671`となった。一定スコアの基準AP=`0.024011`を上回っており、現在の入力・前処理・特徴集約の条件では、リスクラベルと相関する情報をV-JEPA 2の特徴空間から線形に取り出せることが確認できた。

ただしこれは、リスク直前の特徴量変化や将来の事故予測を調べた結果ではない。また、C・閾値の選定と評価を同じvalidationで行っているため、現時点では探索的な結果である。

## 1. 何を確かめたかったか

確認したかったことは、次の問いである。

> 直前2秒の映像をV-JEPA 2で特徴化したとき、その特徴から、公式ラベル上のリスク区間と非リスク区間を分類できるか。

ここでいう「直前2秒」は、リスク発生時刻の直前2秒ではない。各判定時刻`t`に対する観測窓`[t-2,t)`という意味である。

また、Bは「特徴量の時間的な大きな変化」を直接検出する実験ではない。窓間の差分や変化速度ではなく、各窓の特徴そのものを分類に使っている。

## 2. どのような学習をしたか

処理は次の流れである。

```text
判定時刻 t
  ↓
直前2秒 [t-2, t) の64フレームを入力
  ↓
固定したV-JEPA 2で特徴化
  ↓
特徴を1024次元のglobalベクトルへ集約
  ↓
線形probeでリスクスコアを出力
```

V-JEPA 2の出力は`[32,16,16,1024]`として扱い、空間・時間方向に平均した`[1024]`次元のglobal特徴を主分類器へ入力した。時間方向の特徴`[32,1024]`も保存したが、今回のBの分類器では使用していない。

学習したのは次の部分だけである。

- `StandardScaler`: trainデータだけでfit
- `LogisticRegression(class_weight="balanced")`: trainデータだけでfit
- V-JEPA 2エンコーダ: 固定。再学習なし
- C候補: `0.01, 0.1, 1, 10`
- C選定: validationのframe-level AP最大
- 選択されたC: `0.01`

出力は連続的な分類スコアであり、確率校正済みの「事故確率」ではない。閾値を適用した場合にだけ、リスク／非リスクの二値判定として扱う。

## 3. どのようなデータを準備したか

### 元データと正解ラベル

Nexar Collision Predictionのtrainデータを使用した。positive動画750本、negative動画750本、合計1,500動画を、内部trainとvalidationへ分割した。

各動画の公式メタデータにある`time_of_alert`と`time_of_event`から、公式リスク区間を次の半開区間として定義した。

```text
[time_of_alert, time_of_event)
```

各窓の終了時刻`t`について、`t`がこの区間内なら正例、区間外なら負例とした。positive動画でも区間外の窓は負例、negative動画は全窓が負例である。

### 準備した窓

判定時刻を2.0秒から0.5秒ずつ進め、各時刻に対して過去2秒の64フレームを作成した。完全な窓だけを有効とし、映像はアスペクト比を保ったletterboxと公式processorで前処理した。

| データ | 動画数 | 有効窓数 | 正例窓 | 負例窓 | 用途 |
|---|---:|---:|---:|---:|---|
| 内部train | 1,200 | 86,365 | 1,898 | 84,467 | scaler・probeのfit |
| 内部validation | 300 | 21,554 | 519 | 21,035 | C・閾値選定と評価 |
| 公式test | 0 | 0 | - | - | 未取得 |

## 4. 判定と評価をどう行ったか

判定時は、映像を次のようにスライドさせる。

```text
t=2.0秒   → [0.0, 2.0) の映像 → スコア
t=2.5秒   → [0.5, 2.5) の映像 → スコア
t=3.0秒   → [1.0, 3.0) の映像 → スコア
```

スコアは窓末尾の時刻`t`に対応づけた。評価時には、各窓のスコアを`t <= u < t+0.5`のフレームへ割り当て、公式ラベルと比較した。

連続スコアについてはROC-AUCとAverage Precisionを計算した。閾値付きでは、validation上でF1最大となる閾値を選び、イベント検出率・検出遅延・誤報を集計した。

## 5. どのデータに対して、どの結果が出たか

probeのfitには使っていない内部validationの300動画・21,554窓で結果を算出した。ただし、Cと閾値の選定にも同じvalidationを使っているため、厳密な未使用データ評価ではない。

### validationのフレーム評価

窓スコアをフレームへ割り当てた結果、340,025フレーム行のうち321,931行が有効だった。

| 指標 | B | 一定スコア基準 |
|---|---:|---:|
| 全フレーム行数 | 340,025 | 340,025 |
| 有効フレーム行数 | 321,931 | 321,931 |
| coverage | 0.946786 | 0.946786 |
| 正例フレーム | 7,730 | 7,730 |
| 負例フレーム | 314,201 | 314,201 |
| ROC-AUC | 0.801785 | 0.500000 |
| Average Precision | 0.144671 | 0.024011 |

APは正例率ベースの一定スコア基準の約6.0倍だった。動画単位bootstrap（seed=`42`、1,000回）のAP 95% percentile区間は`[0.112067, 0.189306]`である。

### 閾値付きのイベント評価

validationで選んだ閾値は`0.8996675`、そのときのF1は`0.226540`だった。

| 指標 | 結果 |
|---|---:|
| 評価イベント数 | 154 |
| 完全採点イベント数 | 154 |
| 検出イベント数 | 80 |
| 検出率 | 0.519481 |
| 平均検出遅延 | 1.050530秒 |
| 誤報数 | 161 |
| 誤報率 | 0.015407件 / valid秒 |

## 6. この結果から言えること

現行のV-JEPA 2特徴、2秒窓、global pooling、線形probeという条件では、Nexarのリスク区間と相関する視覚情報を特徴空間から取り出せる。固定した特徴でも、一定スコアよりはリスク区間を順位づけできた。

一方で、これは「特徴量の大きな変化を検出できた」という意味ではない。今回のBは、同じ動画のリスク直前とリスク中の特徴量差分を比較していない。各窓を独立に特徴化し、正例群と負例群を線形分類している。

また、イベント検出率は52.0%、誤報は161件である。したがって、現時点の結論は「実用的な警告器が完成した」ではなく、「凍結特徴をリスク区間分類へ転用できる可能性が、全件validationで支持された」とするのが適切である。

## 7. この実験ではまだ分からないこと

- リスク発生前に特徴量が変化し、将来のリスクを予測できるか
- 窓間の特徴差分や変化速度を使うと性能が上がるか
- 未使用の動画でも同じ性能が出るか
- 何が危険なのか、どの物体・状況が原因なのかを説明できるか
- Nexar以外の自前映像で実用になるか

公式testは未取得であり、validationはC・閾値選定と評価を兼ねている。最終評価では、trainで学習したprobeとvalidationで固定したC・閾値を変更せず、未使用holdoutまたは公式testへ一度だけ適用する必要がある。

## 再現用成果物

以下のパスは実験環境内のローカル生成物を指す。`probe.pkl`、特徴cache、動画別スコアなどは初回公開リポジトリには含めない。B分類モデルを公開する場合は、権利確認後に安全な形式へ変換し、モデルカードを付けた別成果物として用意する。

- 特徴cache: `artifacts/features-p6-full/`
- probe: `artifacts/probe-p6-full/probe.pkl`
- probe metadata: `artifacts/probe-p6-full/metadata.json`
- validation score: `artifacts/scores-p6-full/scores.json`
- evaluation metrics: `artifacts/evaluation-p6-full/metrics.json`
- A/B統合評価: `artifacts/evaluation-p6-full-ab/metrics.json`
- 統合レポート: `artifacts/report-p6-full-ab/report.md`

主なfeature cache keyは`a10e14783dfd1096073b7d1569318372b0e05618c33f2a02096e85b39ba60456`である。

## 次の作業

1. 完了: Aのvalidation全件結果を得て、A/Bを同一フレーム集合で比較した。詳細は[`ab-result.md`](ab-result.md)を参照する。
2. 未使用holdoutまたはNexar公式testを確保する。
3. trainで学習したprobeとvalidationで固定した閾値を、最終データへ固定適用する。
4. 「直前の特徴量変化」や「事故前予兆」を調べる場合は、未来のリスクを正例にした別実験として設計する。
