---
title: V-JEPA 2 による運転映像リスク判定の検証設計
updated: 2026-09-08
status: implementation-ready-with-preflight-gates
---

# V-JEPA 2 による運転映像リスク判定の検証設計

凍結した V-JEPA 2 の特徴量が事故区間の識別に役立つかを Nexar Collision Prediction で検証する。教師あり分類と予測誤差を比較し、事故検出・予兆検出・実環境での有用性を分けて判断する。

実装順序と完了条件は [実装プラン](2026-09-06-implementation-plan.md) を参照する。

## 1. 目的と判断の範囲

最終目標は、自前の走行動画から注意すべき区間を抽出し、運転を振り返る講評を返すこと。Phase 1 では公開データ上の実現可能性だけを検証する。

| 経路 | 問い | 結果から言えること |
|---|---|---|
| B: 凍結特徴 + 線形分類 | 少量の学習可能なパラメータで事故区間を識別できるか | この入力・プーリング条件で線形に取り出せる情報の有無 |
| A: 凍結予測器の予測誤差 | 予測しにくさが事故区間と相関するか | 当該マスク・スコア条件での異常検知の有効性 |

B は性能上限ではない。両経路が失敗しても、特徴量自体が無効と断定せず、入力・プーリング・マスク・評価を切り分ける。両経路が成功しても、Nexar 内の検証を通過したという結論までとし、通常走行への実用性は別途測る。

V-JEPA 2 のマスク予測能力が運転リスクへ転用できるかは仮説である。ロボット操作を条件とする V-JEPA 2-AC の成果を、そのまま今回のモデルの能力として扱わない。

## 2. スコープ

Phase 1 の主課題は、現在までに観測した映像から現在の事故・異常区間を検出すること。出力は時系列スコア、指標、確認用の区間一覧とする。

- エンコーダ・既存 predictor は凍結。B の線形分類器だけ学習する。
- A と B の両経路を評価する。A の成立条件を満たせない場合は未完了・理由を明記する。
- 予兆検出は主評価が完成した後の追加実験。実装必須範囲に含めない。
- GPS、自車操作、自然言語講評、検出器との融合、別モデル比較、Web UI は対象外。

### 現段階の実験解釈（2026-09-08）

今回のPhase 1では、V-JEPA 2をNexarの動画で再学習するのではなく、Metaの事前学習済み重みを固定して、リスク判定への転用可能性を確認する。Nexarの公式`train`（positive 750本、negative 750本）を内部train/validationへ分割し、ラベル付きのvalidationで予備評価・全件評価を行う。公式`test-public`／`test-private`は別の最終評価用データであり、現時点では未取得である。

- **A（predictor）**: 2秒・64フレーム窓の前半48フレームだけから後半16フレームの特徴を予測し、予測特徴と実際の後半特徴（target encoder）の平均L1誤差をリスクスコアとする。これは後半フレームを観測した時点で確定する予測誤差・異常度であり、前半だけで将来の事故確率を即時に出すオンライン予兆モデルとは区別する。
- **B（feature + probe）**: 観測済み64フレーム全体をV-JEPA 2で特徴化し、空間・時間方向にpoolingした特徴をNexarのtrainラベルで学習した線形probeへ入力してスコアを出す。Bは未来フレームの予測ではなく、観測窓の特徴分類である。

現段階の目的は、上記2経路で「観測窓のリスク区間を識別できるか」を比較することとする。この目的には現在のA/B設計が合致しているため、オンライン先行警告へ拡張する場合は別実験として扱い、Phase 1の評価定義は変更しない。

## 3. データと分割

Nexar Collision Prediction の公式 `train` データを使用する。positive 750本、negative 750本の計1,500本を使用し、公式 `test-public`／`test-private` は使用しない。Parquetの `time_of_alert` と `time_of_event` を秒単位の時刻として読み、positiveのリスク区間を `[time_of_alert, time_of_event)` とする。MP4の実fpsとフレーム数は `ffprobe` で確認する。

データの取得・表示・公開は、公式データカードと同梱ライセンスに従う。生データ、フレーム、特徴量cacheはリポジトリへ含めず、取得先と固定revisionを記録する。詳細は [公開方針](PUBLICATION_POLICY.md) を参照する。

### 分割ルール

1. 公式 split を保持する。`metadata_val.json` の名前だけで用途を推測せず、`subset` と配布資料を確認する。
2. 公式 train の動画群から約20%のグループを内部 validation として固定する。動画IDでソート後、`GroupShuffleSplit(n_splits=1, test_size=0.2, random_state=42)`を使う。グループIDは判明している元動画ID、不明なら動画IDとする。実際のID一覧を成果物に保存する。
3. 元動画IDが取得できる場合は同じ元動画のクリップを同一グループにする。公式 train/test をまたぐ重複が見つかった場合は、testを保持してtrain側の該当グループを除外し、公式条件からの差分を記録する。
4. 元動画ID不明の場合はクリップ単位の分割とし、元動画重複を排除できていない制約を記録する。
5. 分割後に窓を作る。同じ動画の重複窓が train/validation/test にまたがらないようにする。

正規化・分類器の学習はtrainのみ。ハイパーパラメータや閾値の選定はvalidationのみ。testを見て設定を選び直さない。

Nexarのnegative動画は、positive動画のリスク区間とは異なるラベル定義である。事故を含まない日常の長時間走行を代表するデータとはみなさず、データカードのラベル定義に基づく参考比較として扱う。

## 4. 時刻・窓・ラベルの契約

内部時刻は秒、区間は半開区間 `[start, end)` に統一する。NexarのParquetに記録された時刻を変換せず保存し、MP4の実fps・フレーム数・時刻配列を `ffprobe` の結果から生成する。動画とメタデータの対応を確認できるまで全件抽出へ進まない。

| 設定 | 初期値 |
|---|---|
| 観測窓 | 過去2.0秒 `[t-2.0, t)` |
| 更新間隔 | 0.5秒 |
| 入力 | 64枚のRGB、256×256 |
| フレーム抽出 | 標本時刻 `q_j = t-2.0 + (j+0.5)×2.0/64`（j=0..63）に、窓内で最も近いフレームを対応。等距離なら早い方 |
| 重複 | 許容し、元フレームID・実効ユニーク枚数を記録 |
| 初期区間 | 完全な2秒窓がない時刻は未採点。ゼロ埋めスコアにしない |
| Bの学習ラベル | 利用可能時刻 `t` が公式異常区間内なら1、それ以外0 |

10fpsの2秒窓には約20枚しかない。64枚への重複サンプリングは入力形状を揃える初期条件であり、時間情報の増加ではない。窓長・入力枚数の変更は別設定として再抽出する。

動画の正規化した開始時刻をv0、終端をv1とし、窓末尾は `t=v0+2.0+k×0.5`（kは0以上の整数、t<v1）に固定する。標本時刻と採用フレームの実時刻を両方保存する。10fps格子上に必要なフレームの欠損がある窓は無効とし、欠損を隣接フレームの複製で隠さない。意図した64枚への重複標本化と、元データ欠損を区別する。

前処理はアスペクト比を保持して256×256に収め、余白を付ける方式を初期値にする。倍率は`min(256/W,256/H)`、寸法は`floor(元寸法×倍率+0.5)`を1..256に収め、PillowのBILINEARでresizeする。中央配置し、余白が奇数なら右・下を1画素多くする。余白はRGB=(0,0,0)。これらを設定・キャッシュキーに記録する。横長映像の左右を失わないための選択であり、学習時のcropとの分布差は残る。公式processorの正規化を使用し、二重resize/cropを避ける。確認画像と実際のtensor形状・レンジを保存する。

### スコアが利用できる時刻

B は窓末尾 `t` で出力する。A は同じ窓の前半から後半を予測しても、誤差を得られるのは後半を観測した `t` である。予測を作った時刻へスコアを遡って付与しない。

フレーム時刻 `u` に対して `t <= u < t+0.5` を満たす予定窓のスコアを割り当てる。その窓が無効・未処理なら未採点とし、前の有効スコアで穴を埋めない。動画終端を越えて保持しない。未来のスコアによる補間・窓全体への逆配布はしない。主評価では平滑化しない。

A/Bの比較は両方が有効な共通フレーム集合で行い、採点対象フレーム数と割合を報告する。各経路単独の評価も別途保存する。推論時間は別途計測し、この時刻定義は計算遅延を含まないことを記載する。

## 5. 経路B: 凍結特徴と線形分類

モデルIDは `facebook/vjepa2-vitl-fpc64-256`。Hugging Face の `get_vision_features()` を用いる。モデルrevision、transformers/torch等のバージョンを動作確認後に固定する。

1. `eval()`、勾配無効化で抽出する。GPUが対応する混合精度とSDPAを候補とし、batch=1から実測する。
2. 出力 `[B, 8192, 1024]` を確認する。token順を確認して `[B, 32, 16, 16, 1024]` に変形する。
3. 全体平均 `[B,1024]` と、空間平均のみの `[B,32,1024]` をFP16で保存する。平均の演算はFP32で行う。
4. 主分類器は `StandardScaler` + L2正則化ロジスティック回帰。入力は全体平均、`class_weight="balanced"`、`C ∈ {0.01,0.1,1,10}`。scalerはtrainでfitする。
5. validationのframe-level AP最大でCを選ぶ。同点なら小さいCを採用。収束警告は記録して解決し、無視しない。
6. trainとvalidationを再結合して再学習せず、選定したtrain学習済みモデルでtestを一度評価する。

線形分類部分はCPUで実行可能。出力は分類スコアであり、確率校正済みの「危険確率」とは呼ばない。時間軸を残した特徴は将来の比較用であり、主実装で追加ヘッドを増やさない。

## 6. 経路A: マスク予測誤差

### 実装の必須条件

Hugging Face の確認時点の実装は、窓全体をエンコードした後でcontextを選択する。これをそのまま未来予測に用いると未来情報が混ざる。AにはMeta公式の、Attentionより前に対象トークンを除去できるエンコーダを使用する。

公式リポジトリの動作確認済みcommitを固定し、重みの取得先とキーを確認する。moving mainを直接依存にしない。context encoder、target encoder、predictorを公式学習時の対応に揃える。公開重みに必要なキーがない場合、target encoderの代用などを黙って行わず、Aの実行を止めて不足を報告する。

### 初期マスク

64フレーム・tubelet_size=2なら時間方向32トークン。先頭24時刻分（48フレーム相当）をcontext、残り8時刻分をtargetとし、各時刻の全空間位置を含める。概ね過去1.5秒→残り0.5秒に相当するが、実時間は保存した標本時刻から求める。

- token IDは時間→高さ→幅の順を実装で確認する。
- context/targetは重ならず、全8192トークンを覆う。
- 境界はtubeletに合わせる。フレーム重複は記録し、別の時間位置で同じ画素が存在する可能性を認める。
- mask tokenは個数ではなく選択IDを固定する。公式predictorの`mask_index=1`を初期値とし、固定commitでこの引数の意味を確認する。

### 処理

```text
RGB窓 → context位置だけを残してエンコード → predictor → 予測target特徴
RGB窓 → target encoderで全体をエンコード → target位置の正解特徴
予測と正解のL1距離 → targetの全位置・特徴次元を平均 → 時刻tのスコア
```

正解特徴には公式学習コードに合わせたLayerNormを適用する。L1の演算・集約はFP32。予測側の正規化を勝手に追加しない。両側に同一の前処理を適用する。

正解側は窓全体を参照してよいが、`t`以降の映像は参照しない。予測側はtarget画素を一切参照してはいけない。target側の画素だけを変更し、予測値が数値誤差の範囲内で不変となるテストを必須にする。

このスコアは事後的に観測した予測誤差であり、未来の事故確率ではない。Aの重みは追加学習せず、主評価は生スコアを用いる。validationラベルで閾値を選んだ場合は「重みは追加学習なし、閾値はラベルで調整」と明記し、完全にラベル不要とは称さない。

## 7. キャッシュと再現性

キャッシュ単位は動画。埋め込み配列は`.npy`、対応する窓情報と抽出設定をJSON等で保存する。各行とwindow_idの対応を固定する。

| 保存対象 | FP16の容量／窓（概算） | 方針 |
|---|---:|---|
| 全8192×1024トークン | 16MiB | 20本以下の診断用途のみ、既定では保存しない |
| 空間平均32×1024 | 64KiB | Bで保存 |
| 全体平均1024 | 2KiB | Bで保存 |
| Aのスコア | 小容量 | スコアとマスク設定を保存 |

キャッシュキーにはデータmanifestのハッシュ、モデル重みrevision、前処理、窓長、標本化、入力枚数、プーリング、dtype、抽出実装versionを含める。Aには公式commit・重みハッシュ・マスク・正規化も含める。ラベルやsplitは別管理とし、実験出力にはそれらのハッシュも残す。

未完成ファイルを有効キャッシュと誤認しないよう一時ファイルから置換し、shape/行数/キーを検証する。異なるキーは別ディレクトリへ保存する。窓長・標本化・前処理・Aのcontextマスク変更は再抽出が必要。

## 8. 評価と成功の解釈

主指標は公式ラベルに対するframe-level ROC-AUCとAP。フレームを連結するmicro評価を主とし、動画単位bootstrap（seed=42、1000回）でpercentile法の95%区間（2.5/97.5百分位）を求める。共通集合のA/Bには同じ動画抽出列を使う。重複抽出した動画はその回数だけフレームを含める。単一クラスの再標本化は無効として数を報告する。無効が多い場合は区間を確定値として示さない。

一定スコアのベースラインを必ず併記する。APは採点集合の正例率が基準となる。ROC-AUC/APに恣意的な成功閾値を事後設定せず、基準との差、区間の幅、失敗例を報告する。APはscikit-learnのaverage precision定義とし、台形PR-AUCと混同しない。

固定閾値での補助評価は、validationの有効フレームでF1最大の閾値を選択し、同点なら高い閾値を採用する。候補はvalidationのユニークなスコアと全陰性用の+∞、判定はscore>=threshold。A/Bそれぞれ選定し、testで固定する。連続した閾値以上のフレームを1アラームとし、陰性フレーム・未採点・時刻欠損で分割する。10fpsなら陽性フレーム時刻から0.1秒の区間を連結し、動画終端で切る。

- 共通評価で事故の全アノテーションフレームが有効な事故を、イベント指標の分母にする。部分的にしか採点できない事故数も別途報告し、未検出と混同しない。単独評価にも同じ完全被覆ルールを使う。
- 事故区間と最初に重なったアラームの遅延と、事故ごとの検出率を報告。事故開始前から継続したアラームは遅延0とし、予兆検出の証拠にはしない。未検出を除いて遅延だけ良く見せない。
- アラーム区間が異常区間と重ならなければ誤報。非異常の有効時間あたりの誤報回数も示すが、Nexar内の参考値と明記する。
- カテゴリ、自車関与、可能なら昼夜等で失敗例を層別化する。ラベルがない属性を推測で正解扱いしない。
- 公式論文との比較は論文版、対象動画一覧、除外条件、教師あり条件、時刻処理を表にする。不一致なら参考比較とする。APの既存値があると仮定しない。
- STAUCは空間的な異常スコアを要するためPhase 1の必須指標に含めない。

## 9. 段階的な実験

1. データ・重み取得・実行環境・アノテーション時刻を確認する。
2. 内部trainの動画IDをソートしてseed=42で並べ替え、その先頭20本で前処理とラベルを目視確認し、特徴抽出とAの漏洩チェック、速度・容量を実測する。t-SNEは任意の可視化であり続行条件にしない。
3. 同じ内部trainの先頭160本と、内部validationを別途ソートしてseed=42で並べ替えた先頭40本を予備集合とし、Bから評価まで通す。不足時は各split内の全件までとし、split間で補充しない。実際のIDを保存し、最低限両クラスが存在することを確認する。公式testは使わない。
4. 同じ部分集合でAを評価する。
5. 実測から全体の時間・容量を見積もり、設定を固定して全件処理・test評価を行う。
6. 経路別の結論と未解決事項を残す。

20本の動作確認と200本の予備評価を、全体testの結果として報告しない。テストを見た後の追加実験は探索的なものとして区別する。

## 10. 将来の予兆検出と講評

予兆タスクでは観測窓は同じく過去だけとし、事故開始時刻をaとして `t < a <= t+H` を別の教師ラベルにする。既に事故中の時刻は予兆評価から除外し、H、開始前の評価対象範囲、検出率、先行時間、誤報の定義を着手前に固定する。主課題の事故ラベルを上書きしない。

自前の通常走行で、撮影条件・誤報回数・見逃しを確認した後、GPS等と実フレームを用いたVLM講評を検討する。GPS速度だけで正確なブレーキ操作時刻や車間時間が分かるとは仮定しない。それらには別の観測・推定と精度評価が必要。

## 11. 参照資料

2026-09-06のレビューで確認した資料。APIと重みは実装時に固定版で再確認する。

- [Meta V-JEPA 2](https://github.com/facebookresearch/vjepa2)
- [Meta encoder: Attention前のmask処理](https://github.com/facebookresearch/vjepa2/blob/main/src/models/vision_transformer.py)
- [Meta学習処理: context/targetと正規化](https://github.com/facebookresearch/vjepa2/blob/main/app/vjepa/train.py)
- [Meta重みの読み込み](https://github.com/facebookresearch/vjepa2/blob/main/src/hub/backbones.py)
- [Hugging Face V-JEPA 2 API](https://huggingface.co/docs/transformers/model_doc/vjepa2)
- [Hugging Face実装](https://github.com/huggingface/transformers/blob/main/src/transformers/models/vjepa2/modeling_vjepa2.py)
- [モデル設定](https://huggingface.co/facebook/vjepa2-vitl-fpc64-256/blob/main/config.json)
